apply plugin: 'com.android.application'

//utils.gradle
apply from: rootProject.getRootDir().getAbsolutePath() + "/utils.gradle"

//realm
apply plugin: 'realm-android'
android {
    compileSdkVersion 25
    buildToolsVersion "25.0.0"

    defaultConfig {
        applicationId "me.halin.testapp"
        minSdkVersion 16
        targetSdkVersion 23
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"

    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    dataBinding {
        enabled = true
    }
    lintOptions {
        abortOnError false
    }
    sourceSets {
        main {
            jniLibs.srcDir 'libs'
            jni.srcDirs = []           //disable automatic ndk-build call
            jniLibs.srcDirs = ['libs']
        }
    }

}

dependencies {
    compile fileTree(dir: 'libs', include: ['.jar', '.so'])
    compile fileTree(include: ['*.jar'], dir: 'libs')
    //facebook rebound框架学习
    //websocket
    //测试框架
    //JUnit下使用mockito时的配置
    //robolectric
    //AndroidInstrumentation测试配置
    //UI测试框架
    compile project(':fundamental')
    //xstream
    compile('com.thoughtworks.xstream:xstream:1.4.7') {
        exclude group: 'xmlpull', module: 'xmlpull'
    }
    //greenDao
    

    testCompile 'junit:junit:4.12'

    compile 'com.android.support:appcompat-v7:25.1.1'
    compile 'com.android.support:recyclerview-v7:25.1.1'
    compile 'com.facebook.rebound:rebound:0.3.8'
    compile 'org.java-websocket:Java-WebSocket:1.3.0'
    compile 'javax.annotation:jsr250-api:1.0'
    compile 'org.greenrobot:greendao:2.2.1'
    compile 'com.android.support:design:25.1.1'
    compile 'com.google.dagger:dagger:2.9'
    compile 'com.android.support.constraint:constraint-layout:1.0.0-beta4'
    testCompile 'junit:junit:4.12'
    testCompile 'org.mockito:mockito-core:1.10.19'
    testCompile 'org.robolectric:robolectric:3.0'
    androidTestCompile 'com.android.support:support-annotations:25.1.1'
    androidTestCompile 'com.android.support.test:runner:0.5'
    androidTestCompile 'com.android.support.test:rules:0.5'
    androidTestCompile 'com.android.support.test.espresso:espresso-core:2.2.2'
    androidTestCompile 'junit:junit:4.12'
    androidTestCompile 'com.google.dexmaker:dexmaker:1.2'
    androidTestCompile 'com.google.dexmaker:dexmaker-mockito:1.2'
    androidTestCompile 'org.mockito:mockito-core:1.10.19'
    annotationProcessor 'com.google.dagger:dagger-compiler:2.9'
}

//beforeProject{
//
//    println"before App Project."
//
//}
//afterProject{
//    println"after App Project."
//
//}

tasks.getByName("build") {
    it.doLast {
        println "$project.name: After build, jar libs are copied tolocal repository"
        copyApk()
    }
}

/*
  导向图完成后,禁止所有任务的release和test
*/
project.afterEvaluate {
//    gradle.allprojects {
//        disableReleaseBuild()
//    }
////    disableTest()
//
//    gradle.allprojects {
//        disableLint(it)
//    }
//
//    //禁用test
//    gradle.allprojects {
//        disableTest(it)
//    }
}
